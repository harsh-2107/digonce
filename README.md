Digonce (React + FastAPI + PostGIS)

This repository contains a React frontend and a FastAPI backend with PostGIS (Postgres) configured via Docker Compose.

Quick start:

```bash
cd digonce
docker-compose up --build
```

Frontend will be available on http://localhost:3000 and backend on http://localhost:8000
# digonce
